kind: "BuildConfig"
apiVersion: "v1"
metadata:
  name: "prevencaofraudes-svc-deploy"
spec:
  source:
    git:
      uri: "https://github.com/csilveir/prevencaofraudes-svc"
  strategy:
    dockerStrategy:
      dockerfilePath: Dockerfile_Deploy